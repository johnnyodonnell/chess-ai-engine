node_modules
dist
.DS_Store

# Training artifacts — kept on asus-nvidia, not in the repo. The exported
# ONNX checkpoint(s) we ship live in public/models/ and are committed.
training/__pycache__/
training/checkpoints/
training/data/
training/logs/

# Self-play run output (snapshots, latest.pt, pool.json, best.onnx, logs).
runs/
