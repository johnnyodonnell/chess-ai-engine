[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "chess_rs"
version = "0.1.0"
description = "Native chess board + AlphaZero encoding for self-play"
requires-python = ">=3.12"

[tool.maturin]
features = ["pyo3/extension-module"]
