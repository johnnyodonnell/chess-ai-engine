python-chess>=1.11
numpy>=2.0
onnx>=1.20
onnxruntime>=1.20
# torch is installed separately from the PyTorch nightly cu128 index:
#   uv pip install --index-url https://download.pytorch.org/whl/nightly/cu128 torch
